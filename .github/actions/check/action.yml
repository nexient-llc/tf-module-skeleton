name: "Make Check Action"
description: "Run Make Check"
author: "Nexient DevSecOps"

runs:
  using: "composite"
  steps:
    - name: make configure
      run: |
        IS_PIPELINE=true make configure
      shell: bash
    - name: make check
      run: |
        make check
      shell: bash
